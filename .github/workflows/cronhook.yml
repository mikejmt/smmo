name: Trigger Bash Script

on:
  repository_dispatch:
    types: [trigger-bash-script]

jobs:
  run-bash-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run bash script
        run: |
          bash /path/to/your/bash/script.sh
